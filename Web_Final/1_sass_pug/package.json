{
  "name": "26_sass",
  "version": "1.0.0",
  "description": "Ejemplo del uso de SASS",
  "main": "index.js",
  "scripts": {
    "start": "sass sass/:css/ --watch",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass sass/:css/ --watch  --no-source-map",
    "pug": "pug -w -P -p ./pug/partials -o ./pug/options.json ./pug/templates -o ./"
  },
  "keywords": [],
  "author": "Hitomi Aoki",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "sass": "^1.22.12",
    "pug": "^2.0.3"
  }
}
